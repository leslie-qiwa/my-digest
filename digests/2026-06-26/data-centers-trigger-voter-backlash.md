# Data centers trigger voter backlash

of this text for your own reference (not publication), I can do that, but flagged as unverified content, not as a digest entry.

This also looks unrelated to the my-ops repo you're working in — the repo's digest engine (`scripts/digest_engine/`) pulls from real provider feeds rather than pasted text. If you're testing that pipeline, happy to help there instead.

Which direction do you want?
